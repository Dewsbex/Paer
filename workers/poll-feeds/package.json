{
  "name": "paer-poll-feeds-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker cron that invokes the Supabase poll-feeds Edge Function on a schedule.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  }
}
